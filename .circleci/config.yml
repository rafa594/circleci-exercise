version: 2
jobs:
    dockerbuild:
        machine: true
        steps:
            - checkout
            - run: |
                ls -l
                ls -l ../
                

workflows:
    version: 2
    build-workflow:
        jobs:
            - dockerbuild:
                filters:
                    branches:
                        only: master